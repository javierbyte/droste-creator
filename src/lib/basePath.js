// The site is served from javierbyte.github.io/droste-creator and mounted at
// javier.xyz/droste-creator by a vercel rewrite, so every asset lives under this
// prefix. Imported by next.config.mjs as well, so the value is defined once.
export const BASE_PATH = '/droste-creator';
